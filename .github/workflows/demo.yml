name: Update
on:
  push: {}

  schedule:
    - cron:  '* 13 * * *'

jobs:
  build:
    name: Demo
    runs-on: ubuntu-latest
    steps:
    - name: Check out
      uses: actions/checkout@v1
      with:
        fetch-depth: 1
    - name: Updating stack
      uses: gimlet-io/gimlet-stack-updater-action@7b5e8c295d53a2d87ce8a7c27d4943646a561db8
      with:
        config: 'fixture/stack.yaml'
