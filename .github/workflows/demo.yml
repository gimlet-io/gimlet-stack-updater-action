name: Update
on:
  push: {}

  schedule:
    - cron:  '* 13 * * *'

jobs:
  build:
    name: Demo
    runs-on: ubuntu-latest
    steps:
    - name: Check out
      uses: actions/checkout@v1
      with:
        fetch-depth: 1
    - name: Updating stack
      uses: gimlet-io/gimlet-stack-updater-action@1aa12959a41d49c8aa75ecf67c702460c5d045db
      with:
        config: 'fixture/stack.yaml'
